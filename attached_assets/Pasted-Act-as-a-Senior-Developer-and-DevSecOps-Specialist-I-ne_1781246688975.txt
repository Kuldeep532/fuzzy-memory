Act as a Senior Developer and DevSecOps Specialist. I need to clean up my project workspace to remove confusion, but I want to ensure that no core files required for compiling, building, or running the application are damaged or deleted. 

Project Context & Special Instructions:
This is a Jetpack Compose (Android) project. However, the workspace currently contains unrelated React/TypeScript files and directories that are causing confusion. 
- Please identify and completely remove all React, Node, and TypeScript related files and folders shown in the project tree, including: node_modules/, src/hello.ts, package.json, tsconfig.json, tsconfig.tsbuildinfo, and .npmrc.
- Ensure all core Android/Gradle build files (build.gradle.kts, settings.gradle.kts, gradlew, gradlew.bat, gradle/, gradle.properties, proguard-rules.pro) and the README.md remain completely untouched and protected.

Please perform a safe cleanup process inside the directory by strictly adhering to these rules:

1. Safe Target Deletion Only:
   - Identify and delete ONLY the specified React/TypeScript files, and known temporary, redundant, or generated files that do not affect the Android source code or compilation.
   - Target files explicitly for deletion: *.tmp, *.log, .DS_Store, thumbs.db, any empty/leftover temporary directories, and the entire React/Node ecosystem files mentioned above.
   - Do NOT touch or delete any core Android project code files, configuration files, Gradle wrappers, or assets.

2. Verify Build Integrity:
   - Before running the cleanup script, double-check that critical Android project structures and Gradle configuration files are completely locked and protected from deletion.

3. Secure Key Management (GitHub Secrets):
   - Confirm that the production signing key (keystore) is NOT saved anywhere in the repository files. It must be generated or fetched dynamically from GitHub Secrets (secrets.ENCODED_PLAY_STORE_KEYSTORE) only at the time of the CI/CD pipeline execution.

Provide a clear log of exactly which unusable temporary and React/TypeScript files were deleted, and confirm that the core Jetpack Compose project remains 100% intact and untouched.
