NEXUSPLUS EXECUTION DIRECTIVE

You are continuing an existing NexusPlus Compose Multiplatform project, not starting a new one. A large migration, architecture cleanup, Weather system, News & ePapers foundation, Science features, Feature Registry foundation, accessibility improvements, and shared architecture work have already been completed. Do not restart migration, do not rebuild completed modules, and do not perform another full-project rewrite. Continue from the current state and finish the remaining work.

Operate in fully autonomous mode. Do not ask questions, do not request clarification, do not wait for confirmation, do not generate TODOs, placeholders, mock implementations, fake APIs, temporary solutions, or planning reports. Create an internal implementation plan silently and immediately begin execution.

The highest priority is completing the remaining Compose Multiplatform migration. Verify that all possible UI, navigation, repositories, models, services, utilities, feature screens, accessibility components, theme logic, API clients, and shared business logic live in commonMain. Only platform-specific functionality should remain inside androidMain and iosMain. Shared screens, navigation, accessibility, theming, and state management must compile and function correctly on both Android and iOS.

After migration verification, complete Google Sign-In. The project uses GitHub Actions and google-services.json is reconstructed from encrypted CI/CD secrets during builds. Do not assume the file is missing and do not generate replacement files. Audit the secret reconstruction flow, Firebase initialization, Gradle integration, OAuth configuration, Credential Manager integration, Google Identity Services integration, token validation, token decoding, session restoration, silent sign-in, sign-out, and authentication persistence. Remove all remaining authentication placeholders and ensure production-ready sign-in behavior.

Next, complete the speech system and standardize it under the name "Nexus Auto Speech Engine". Remove legacy naming, duplicated labels, technical wording, and inconsistent terminology. The settings interface must provide language selection, engine selection, voice selection, speech rate, pitch, volume boost, and auto optimize. Detect installed engines automatically, persist settings, improve low-volume output, and implement automatic tuning based on device performance, language, and engine capabilities.

Complete OCR integration using Google ML Kit. Convert the Image Reader into a full OCR-enabled reader supporting JPG, PNG, WEBP, and HEIC images with text extraction, search, copy, save, share, and read-aloud capabilities. Complete OCR support inside the Document Reader for PDF, scanned PDF, and image-based PDF files with extraction, search, copy, export, sharing, and speech playback.

Implement a centralized NexusSoundManager. Add lightweight sound feedback for navigation, dialogs, selections, toggles, success events, errors, button interactions, feature launches, and page transitions. The Document Reader must include page-turn audio for forward and backward navigation without interfering with speech playback. Add a Sound Effects setting with ON/OFF control and persistent storage. If sound assets are not present, automatically acquire legally distributable public-domain, CC0, or commercially usable royalty-free assets. Do not use copyrighted, proprietary, or restricted-license sounds. Optimize assets for Android and iOS and integrate them into the shared architecture.

Finalize the Feature Registry and accessibility systems. Feature counts must always be calculated dynamically at runtime and never hardcoded. Remove duplicated announcements and accessibility noise. Screen readers should announce only meaningful content and clean feature names.

Weather, News & ePapers, and Science modules already exist and should not be rebuilt. Verify and complete any remaining work such as offline caching, bookmark persistence, accessibility validation, theme validation, and final integration. News and ePapers must continue using public RSS feeds, public newspaper feeds, and publicly accessible online sources rather than paid news APIs.

Finish the remaining system improvements including AdMob cleanup, navigation cleanup, proper back-stack behavior, theme consistency, accessibility validation, About page updates, Privacy Policy updates, Encryptor improvements, and Help Walls crash fixes. Ensure that banners appear only when ads successfully load and that empty ad placeholders never appear.

Throughout the project, follow Material 3, Dynamic Color, Light Theme, Dark Theme, accessibility best practices, and Compose Multiplatform standards. Prefer commonMain by default. Avoid duplicate implementations and preserve all existing working functionality.

Do not stop after analysis. Continue implementation automatically until all remaining work is complete. At the end, provide a migration report, modified files list, new files list, Android build status, iOS build status, remaining issues, and a complete changelog.
