You are an advanced, full-stack autonomous software architect and senior Android developer specializing in digital accessibility (WCAG / TalkBack conformance) and modular system design. Your objective is to build a complete, production-ready, feature-rich application named "Nexus Plus" using Jetpack Compose and Compose Multiplatform.

The application must be completely self-contained with absolutely NO Web Interfaces (No WebViews, no web-based dashboards). The primary architecture, UI, and optimization must target a native Android Interface using Jetpack Compose, while structuring the shared codebase so it can be easily compiled for other cross-platforms in the future via a multiplatform compiler.

The entire codebase must be 100% accessible, structured, and fully compatible with the TalkBack screen reader.

---
### [CRITICAL CONFIGURATION: SCOPE FLEXIBILITY & GRADUAL SCALABILITY]
If generating all 30+ features simultaneously is not technically possible due to context window limitations, processing complexity, or token constraints, YOU MUST focus on a highly stable core MVP (Minimum Viable Product). Start by successfully implementing the core authentication gateway, navigation architecture, accessibility gestures, the legal trust modules, and the first 6 priority features. Ensure the framework is strictly modular so remaining utilities can be added seamlessly later.
---

### 1. FUTURE-READY FIREBASE AUTHENTICATION & WELCOME GATEWAY
Before entering the main application tabs, the user must be presented with a beautifully structured, native Welcome/Login Screen. The authentication architecture must be fully built-in so that Firebase can be fully activated in the future without changing any UI or navigation code:
- **Sign In with Google Component:** Implement a production-ready, highly accessible "Sign In with Google" custom button layout. Include the entire authentication flow architecture (Sign-In Intents, result callbacks, and state mapping) pre-configured inside the presentation layer, mapping cleanly to a stubbed `FirebaseAuthRepository`. When the real Firebase SDK dependencies are attached later, this code must function automatically without rewriting UI logic.
- **Login as Guest System:** Provide a prominent "Continue as Guest" alternative flow. When tapped, an accessible text field emerges prompting the user to provide ONLY their name. Upon submitting, the session state initializes as an unauthenticated guest user with their customized guest profile name, granting immediate entry to the dashboard.

---

### 2. CORE GATEKEEPER LOGIC (DYNAMIC FEATURE RESTRICTION ENGINE)
To maintain security and monetize premium features, implement a centralized configuration engine called `NexusGatekeeper`.
- **Restriction Mapping:** Define a robust capability matrix where specific modules or features (e.g., AI Image Generator, IPTV Streamer) can be marked as restricted for Guest Users.
- **Interception Logic:** When a user triggers the Double-Tap gesture to open any feature inside the "All Features Tab", the `NexusGatekeeper` must intercept the call. If the feature is restricted and the current session is registered as a "Guest User", the application must block navigation and dynamically show a clear, accessible dialog or bottom sheet stating that this specific feature requires full Google Authentication. TalkBack must explicitly announce the restriction state.

---

### 3. CORE NAVIGATION ARCHITECTURE (BOTTOM NAVIGATION TABS)
Once passing the Welcome Gateway, the application must implement a clean, accessible Bottom Navigation bar containing exactly three primary tabs:
1. **Favorites Tab:** Displays a dynamic, sorted list of all features bookmarked by the user. Show an accessible empty-state layout with TalkBack instructions if empty.
2. **All Features Tab:** Displays the full grid/list of the 30+ modular features. At the top, include a global, accessible Action Menu/Header for filtering, searching, or batch-management.
3. **More Tab:** Hosts application settings, configuration profiles, legal compliance modules, and centralized external developer links.

---

### 4. TALKBACK SUPPORTED CUSTOM GESTURES & FAVORITES MANAGEMENT
For every feature item listed inside the "All Features Tab", implement custom interaction hooks:
- **Double-Click (Standard Tap/Double-Tap for TalkBack):** Intercepted by `NexusGatekeeper` to check access rights, then instantly launches the selected feature module if permitted.
- **Long Press (Double-Tap & Hold for TalkBack):** Instantly triggers an accessible Context Action Menu containing an explicit option to "Add to Favorites" or "Remove from Favorites".
- **TalkBack Optimization:** Every interactive card must utilize `Modifier.clearAndSetSemantics` with specific `contentDescription` properties. Adding or removing items must trigger a native accessibility announcement (e.g., "Feature added to favorites").

---

### 5. MANDATORY DEVELOPER ATTRIBUTION & CENTRALIZED SOCIAL MEDIA LINKS
Every single legal and informational screen within the application—specifically including the "About Us", "Privacy Policy", and "Terms & Conditions" pages—MUST explicitly display and credit the official developer name: **"Nexus Wave Technologies"**.
- **Centralized Social Media Handles:** To maintain a clean UI, the social media link handles (GitHub, LinkedIn, Twitter/X, Official Website) must be implemented cleanly as a dedicated component placed strategically ONLY within the **"More Screen"** dashboard menu and at the bottom of the **"About Us"** page.
- **Intent Execution:** Clicking these handles must trigger an external native Intent wrapper to launch the respective platform cleanly.

---

### 6. LEGAL COMPLIANCE & TRUST MODULES (WITH BRANDING INTEGRATION)
Generate complete, professionally worded user-facing screens natively in Jetpack Compose (No WebViews allowed):
- **About Us Screen:** App vision, versioning data, and the declaration: "Developed and Maintained by Nexus Wave Technologies", with social handles at the bottom.
- **Privacy Policy Screen:** Explicit clauses on how API data is processed, stating: "Nexus Wave Technologies ensures 100% data privacy, non-collection of personal data, and zero remote tracking logs."
- **Terms & Conditions Screen:** User responsibility clauses, fair use policy for the AI tools and streaming, and a strict limitation of liability clause legally protecting "Nexus Wave Technologies".

---

### 7. CORE FEATURE CHRONOLOGY & PLANNING (PRIORITY LIST)
- **Feature 1: Online Radio Player (Radio Browser API):** Fetch stations from `https://at1.api.radio-browser.info/json/stations/search` via Android Media3 ExoPlayer with complete TalkBack state announcements.
- **Feature 2: Native PDF Reader Module:** Local PDF renderer using Android's native `PdfRenderer` on a Jetpack Compose `Canvas` with gesture-based zoom and voice-assisted page index announcements.
- **Feature 3: AI Image Generator (Pollinations AI API):** Text-to-image interface using `https://image.pollinations.ai/p/{prompt}` with style modifiers, an elegant loading shimmer, and gallery saving features.
- **Feature 4: Nexus Auto Speech Engine (Dynamic TTS):** Built-in `TextToSpeech` engine integrated with an automatic language shooting character-block analyzer that automatically switches the TTS engine locale (e.g., Hindi, English, Spanish) before synthesis begins.
- **Feature 5: IPTV / Live TV Player (GitHub M3U Repositories):** Parse raw M3U playlists from GitHub repositories and render video directly via Android Media3 `PlayerView`.
- **Feature 6: Online Music Streaming (JioSaavn API Wrapper):** Connect to public JioSaavn API wrappers for audio streaming, integrated with the Android lock-screen and notification controls.
- **Features 7 to 30+: Extended Utilities Framework:** Plan and generate modular interfaces for remaining features (File Manager, Currency Converter, Encrypted Notes, QR Scanner, etc.) registered within the Koin DI module, dynamic access settings, and navigation graph.

---

### 8. NATIVE ANDROID UI & DESIGN CONSTRAINTS
- 100% native Android look-and-feel using Material Design 3.
- Use explicit sizing (dp/sp), strict dark/light theme definitions, and fully responsive layouts that adapt perfectly to multiple Android screen sizes (phones, tablets, foldables). Absolutely no HTML/CSS components permitted.

### 9. OUTPUT DELIVERABLE
Generate the entire structural codebase including `build.gradle.kts`, Welcome/Login Screen architecture, Google Sign-In stubs, Guest session managers, `NexusGatekeeper` logic interception layer, navigation routes setup for the three tabs, TalkBack gesture handlers, DI configurations, and complete Kotlin files for the core priority features and legal modules now. Do not use placeholders or "// TODO" comments; write the complete functional code.
