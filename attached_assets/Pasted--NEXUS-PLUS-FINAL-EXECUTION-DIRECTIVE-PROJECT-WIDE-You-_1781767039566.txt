# NEXUS PLUS — FINAL EXECUTION DIRECTIVE (PROJECT-WIDE)

You are a Senior Kotlin Multiplatform, Android, Jetpack Compose, Media3, Architecture, Performance, and Code Quality Engineer.

You must perform a COMPLETE PROJECT AUDIT, BUILD RECOVERY, ARCHITECTURE VALIDATION, FEATURE IMPROVEMENT, AND FINAL PRODUCTION HARDENING of the entire Nexus Plus codebase.

═══════════════════════════════════════════════
PROJECT SCAN RESULTS (ALREADY IDENTIFIED)
═══════════════════════════════════════════════

The project scan has already detected several issues that MUST be fixed.

composeApp/build.gradle.kts:

Issue 1:
Invalid sourceSet declaration:

val iosX64() by getting

This is invalid Kotlin DSL syntax and causes build failures.

Fix:
Use modern KMP sourceSet syntax:

val iosX64Main by getting

or migrate completely to the latest Kotlin Multiplatform sourceSet structure.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue 2:

Deprecated:

kotlinOptions {}

Detected in:

• composeApp module
• shared module

Fix:

Migrate all usages to:

compilerOptions {}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue 3:

Potential KMP migration issue.

Detected:

androidTarget {
    compilations.all {
        kotlinOptions { ... }
    }
}

Modernize the Android target configuration according to the Kotlin version used by the project.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue 4:

Potential sourceSet inconsistency between:

composeApp
shared

Validate:

• commonMain
• androidMain
• desktopMain
• iosMain
• iosX64Main
• iosArm64Main
• iosSimulatorArm64Main

Remove invalid declarations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue 5:

Cross-platform structure requires validation.

Several Android-only dependencies exist inside composeApp.

Audit and ensure:

• Shared logic remains in commonMain
• Android-only code remains in androidMain
• iOS-compatible logic is not blocked by Android implementations

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue 6:

Media architecture requires review.

Media3 is already present.

Audit for:

• duplicate playback logic
• separate audio/video player implementations
• playback lifecycle issues
• state restoration issues

Consolidate safely where appropriate.

═══════════════════════════════════════════════
PRIMARY OBJECTIVE
═══════════════════════════════════════════════

The mission is complete ONLY when:

✓ All build errors are fixed
✓ All compilation errors are fixed
✓ All KMP issues are fixed
✓ All navigation issues are fixed
✓ All audio issues are fixed
✓ All media playback issues are fixed
✓ Nexus OTT is implemented
✓ Duplicate files are removed
✓ Duplicate screens are removed
✓ Unused modules are removed
✓ APK size is optimized
✓ Existing functionality is preserved
✓ Debug build succeeds
✓ Release build succeeds

═══════════════════════════════════════════════
GLOBAL APPLICATION RULE
═══════════════════════════════════════════════

Every rule in this directive applies to:

• Every module
• Every screen
• Every feature
• Every ViewModel
• Every repository
• Every service
• Every navigation graph
• Every storage feature
• Every media feature
• Every audio feature
• Every AI feature
• Every settings feature

No exceptions.

═══════════════════════════════════════════════
MINIMAL MODIFICATION RULE
═══════════════════════════════════════════════

Do NOT perform unnecessary large-scale rewrites.

Before making any change ask:

"Can this be solved safely with a smaller change?"

Always prefer:

✓ Smallest safe fix
✓ Lowest-risk improvement
✓ Highest compatibility solution

Avoid:

✗ unnecessary rewrites
✗ unnecessary architecture replacement
✗ unnecessary dependency replacement
✗ unnecessary UI redesign

Only make major changes when absolutely required.

═══════════════════════════════════════════════
FEATURE PRESERVATION RULE
═══════════════════════════════════════════════

Do NOT remove working features.

Do NOT downgrade functionality.

Do NOT simplify advanced functionality merely to make builds pass.

If a feature works:

Keep it.

If a feature is broken:

Fix it.

If a feature is outdated:

Modernize it carefully.

Every feature must be:

✓ Preserved
✓ Improved
✓ Stabilized
✓ Optimized

═══════════════════════════════════════════════
SCREEN STRUCTURE RULE
═══════════════════════════════════════════════

All screen names must be:

✓ Predictable
✓ Consistent
✓ Feature-based
✓ Easy to locate
✓ Easy to maintain

Allowed examples:

FileManagerHomeScreen
FolderBrowserScreen
FileDetailsScreen
AIHomeScreen
AIChatScreen
AudioPlayerScreen
OTTHomeScreen
OTTPlayerScreen
SecurityHomeScreen

Forbidden examples:

HomeScreenV2
NewScreen
UpdatedScreen
FinalScreen
CopyScreen
TempScreen

After creating improved screen structures:

✓ Delete obsolete screens
✓ Delete duplicate screens
✓ Delete unused screens
✓ Update imports
✓ Update navigation
✓ Update routes

No duplicate screens should remain.

═══════════════════════════════════════════════
NAVIGATION RULE
═══════════════════════════════════════════════

Audit every navigation flow.

Requirements:

✓ Proper back navigation
✓ Proper state restoration
✓ SavedStateHandle support
✓ Predictable back stack behavior

Back button must:

Return only to the previous screen.

Never close entire workflows unexpectedly.

Never destroy user progress unnecessarily.

═══════════════════════════════════════════════
KOTLIN MULTIPLATFORM RULE
═══════════════════════════════════════════════

Perform a complete platform audit.

Move to commonMain when practical:

• business logic
• repositories
• models
• use cases
• shared services

Keep Android-only code only inside androidMain.

Examples:

Android-only:

• ML Kit
• Android Intents
• Android Widgets
• Android FileProvider
• Android-specific APIs

Everything else should be migrated toward maximum KMP compatibility when safe.

Do NOT break Android functionality.

Do NOT force impossible iOS migrations.

═══════════════════════════════════════════════
UNIFIED MEDIA PLAYER RULE
═══════════════════════════════════════════════

Perform a complete media audit.

Current objective:

Create one reliable media playback architecture capable of handling both audio and video efficiently.

Requirements:

✓ Shared playback engine
✓ Shared playback state
✓ Shared queue management
✓ Shared media session
✓ Shared playback controls
✓ Better performance
✓ Better lifecycle handling

Use Media3 wherever practical.

VIDEO SUPPORT:

✓ MP4
✓ HLS
✓ DASH (if already supported)
✓ Subtitles
✓ Playback speed
✓ Resume playback
✓ Continue watching
✓ Fullscreen
✓ PiP

AUDIO SUPPORT:

✓ Music playback
✓ Voice playback
✓ Background playback
✓ Notification controls
✓ Lock screen controls
✓ Audio focus handling
✓ Queue support
✓ Playlist support
✓ Sleep timer

Avoid maintaining duplicate playback systems if a shared implementation can safely support both.

Do NOT remove existing playback features.

═══════════════════════════════════════════════
AUDIO SYSTEM RULE
═══════════════════════════════════════════════

Audit all audio assets.

Locate:

• music
• voice
• notifications
• sound effects

Organize:

audio/music
audio/voice
audio/system
audio/notifications
audio/cache

Fix:

✓ broken references
✓ playback issues
✓ routing issues

Remove:

✓ duplicate audio assets
✓ unused audio assets

Without breaking existing functionality.

═══════════════════════════════════════════════
NEXUS OTT RULE
═══════════════════════════════════════════════

Create a dedicated Nexus OTT feature.

Use only legally distributable content sources.

Primary sources:

• Internet Archive
• Dailymotion public content APIs or publicly available embeddable content
• Other publicly available legal content sources with redistribution rights

Before displaying content verify:

✓ rights metadata
✓ licensing metadata
✓ public-domain status
✓ Creative Commons status
✓ redistribution permissions
✓ platform compliance

Allow:

✓ Public Domain
✓ Creative Commons
✓ Legally distributable content

Block:

✗ copyrighted content without permission
✗ pirated content
✗ DMCA-restricted content
✗ unverified content

Implement:

ContentComplianceEngine

If content cannot be verified:

DO NOT DISPLAY IT.

OTT Features:

✓ Search
✓ Categories
✓ Details pages
✓ Watch history
✓ Continue watching
✓ Media3 playback
✓ Resume playback

═══════════════════════════════════════════════
DUPLICATE CLEANUP RULE
═══════════════════════════════════════════════

Identify and safely remove:

• duplicate screens
• duplicate resources
• duplicate assets
• duplicate classes
• duplicate dependencies

Before deleting:

Verify:

✓ not referenced
✓ not required
✓ not actively used

Never delete blindly.

═══════════════════════════════════════════════
NEWS MODULE RULE
═══════════════════════════════════════════════

Audit all news-related functionality.

Remove only:

✓ unused modules
✓ duplicate modules
✓ dead code

Keep active functionality intact.

═══════════════════════════════════════════════
PERFORMANCE RULE
═══════════════════════════════════════════════

Improve:

✓ startup speed
✓ memory usage
✓ navigation responsiveness
✓ compose performance
✓ media loading
✓ storage operations

Remove:

✓ dead code
✓ redundant state handling
✓ unnecessary recompositions

Only when safe.

═══════════════════════════════════════════════
APK SIZE OPTIMIZATION RULE
═══════════════════════════════════════════════

Reduce APK size safely.

Apply:

✓ R8
✓ ProGuard
✓ resource shrinking
✓ dependency cleanup
✓ asset cleanup

Remove only unused resources.

Never remove resources required by active features.

═══════════════════════════════════════════════
MISSION COMPLETION ENFORCEMENT RULE
═══════════════════════════════════════════════

Do NOT stop after:

✗ fixing only build errors
✗ creating only screens
✗ creating only modules
✗ generating only reports
✗ performing only analysis

Continue until all requested work is completed.

Do NOT reduce scope.

Do NOT classify tasks as too large merely because they touch many files.

Do NOT skip refactors because they affect multiple modules.

Do NOT leave:

TODO
FIXME
Placeholder
Coming Soon
Stub
Mock-only implementations

All created components must be production-ready.

If even one required item remains unfinished:

THE TASK IS NOT COMPLETE.

═══════════════════════════════════════════════
FINAL VALIDATION
═══════════════════════════════════════════════

Before completion verify:

✓ assembleDebug succeeds
✓ assembleRelease succeeds
✓ no compilation errors
✓ no unresolved references
✓ no duplicate screens
✓ no duplicate files
✓ audio works correctly
✓ media playback works correctly
✓ navigation works correctly
✓ OTT works correctly
✓ KMP structure remains stable
✓ Android-specific features remain functional

Success is defined as:

A fully functional, production-ready Nexus Plus application with all requested fixes, improvements, validations, media upgrades, audio integration, navigation stability, KMP compatibility improvements, and Nexus OTT implementation completed without breaking existing functionality.