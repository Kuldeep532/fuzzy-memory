Act as an elite Senior Enterprise Mobile Architect and Compiler Engineer. I do not have a laptop to run terminal commands, and I cannot commit any CI/CD .yml pipeline files to GitHub. 

CRITICAL CONSTRAINT: Completely delete, bypass, and remove any reference to CI/CD .yml files or GitHub Action pipelines. Instead, provide a pure source-code shortcut via a standalone Gradle task to handle the entire compilation and keystore generation locally or on a remote build server. 

Implement a 100% unified, zero-configuration, pixel-perfect architecture using Compose Multiplatform and Material Design 3 that looks and behaves identically on iOS, Android, Desktop, and Web without any platform-specific UI rendering discrepancies.

Generate and separate your response into the following exact file implementations. Provide complete, fully-written source code without placeholders, summaries, or TODOs.

---
FILE 1: shared/build.gradle.kts
- Provide the ultimate alternative shortcut to .yml files by writing a complete, self-contained Gradle task named "autoGenerateKeystore".
- This task must automatically execute during the remote compilation phase to check for 'release-keystore.jks'. If missing, it must programmatically generate a standard, valid production JKS keystore utilizing standard Java Security libraries (java.security.KeyStore and KeyPairGenerator).
- Passwords and aliases must dynamically fallback to environment variables or safe defaults so no terminal interaction or laptop is required.
- Hook this task to execute automatically before any package/assemble tasks.

---
FILE 2: shared/src/commonMain/kotlin/com/app/ui/AutoUniversalFormX.kt
- Write the full production implementation of "Form X" using Compose Multiplatform and Material Design 3.
- It must feature an integrated, automatic reactive state validation engine. All inputs, text fields (Email, Password, Antibiotic Web Specification Data), and actions must implicitly and automatically lock, unlock, or adapt based on content validity instantly across ALL platforms (iOS, Android, Desktop, Web) simultaneously without requiring any manual switches or toggle UI.
- Use strict Material 3 structures (Surface, OutlinedTextField, Button) with unified typography and explicit shapes to guarantee a pixel-perfect, identical layout across both mobile operating systems and desktop/web views, completely eliminating layout or compatibility bugs.

Provide only the fully written source code blocks for these components so I can directly paste them into my file tree via the GitHub web interface.
