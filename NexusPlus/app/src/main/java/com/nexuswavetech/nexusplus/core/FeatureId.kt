package com.nexuswavetech.nexusplus.core

enum class FeatureId {
    // ── Original 30 ──────────────────────────────────────────────────────────
    RADIO_PLAYER,
    PDF_READER,
    AI_IMAGE_GENERATOR,
    NEXUS_TTS,
    IPTV_PLAYER,
    MUSIC_STREAMING,
    FILE_MANAGER,
    CURRENCY_CONVERTER,
    ENCRYPTED_NOTES,
    QR_SCANNER,
    WEATHER,
    UNIT_CONVERTER,
    CALCULATOR,
    FLASHLIGHT,
    COMPASS,
    SPEEDOMETER,
    ALARM_CLOCK,
    STOPWATCH,
    WORLD_CLOCK,
    CONTACT_BACKUP,
    VOICE_RECORDER,
    SCREEN_RECORDER,
    APP_LOCKER,
    WIFI_ANALYZER,
    BATTERY_MONITOR,
    STORAGE_ANALYZER,
    TASK_MANAGER,
    CLIPBOARD_MANAGER,
    TRANSLATION_ENGINE,
    BARCODE_GENERATOR,

    // ── New Advanced Features (v1.1) ─────────────────────────────────────────
    TEXT_ENCRYPTOR,
    TEXT_TRANSLATOR,
    OBJECT_DETECTOR,
    COLOR_DETECTOR,
    SMART_IMAGE_EDITOR,
    HASH_GENERATOR,
    PASSWORD_GENERATOR,
    BASE64_TOOL,
    MORSE_CODE,
    NUMBER_SYSTEM,
    JSON_FORMATTER,
    REGEX_TESTER
}
